# Set

```js
// 初始化Set
let set = new Set();

// 初始化一个包含1,2,3元素的集合
let set = new Set([1,2,3]);

// 获取长度
set.size;

// 判断集合是否为空
set.size === 0;

// 判断是否存在某个元素k
set.has(k);

// 添加一个元素4
set.add(4);

// 删除一个元素1
set.delete(1);

// 清除所有元素
set.clear();

// 返回所有的值
set.values();
```

